package souza.home.com.pokedexapp.data.pokedex.local.model

import androidx.room.Entity

/*
@Entity
data class PokemonProperty constructor (
    var id : String, // change to string // was int
    var abilities : MutableList<PokeAbilities>, // was mutable list of poke abilitits
    var name: String,
    var height: Int,
    var sprites: PokeSprites, // was pokesprites
    var stats: List<PokeStats>, // was list of pokestats
    var types: MutableList<PokeTypes>, // was mutable list of types
    var weight: Int = 0
)*/
